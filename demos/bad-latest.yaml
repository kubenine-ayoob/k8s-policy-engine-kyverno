apiVersion: v1
kind: Pod
metadata:
  name: bad-latest
  namespace: ayoob-kyverno
  labels:
    app.kubernetes.io/name: demo
spec: 
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        memory: 256Mi
